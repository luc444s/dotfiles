# Shared git config (user name/email intentionally left blank — set in .gitconfig.local).
[credential "https://github.com"]
	helper = gh auth git-credential

[credential "https://gist.github.com"]
	helper = gh auth git-credential

[include]
	path = ~/.gitconfig.local
